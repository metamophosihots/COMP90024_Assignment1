可能存在的问题：
1.如果存在转发twitter的情况，应该怎么办？（我并没有在mini的JSON文件中看到这种情况，只是猜测）
2.如果同一条推特内一个情绪词出现两次应该怎么办？
3.能否添加一个常用词禁用表来提速，屏蔽I, am, He, is这样常用的无情绪意义的词
4.



程序主要步骤：
1. 横纵向的网格分配函数
经度分区数 = （经度 - 114.7）/ 0.15
if 经度分区数 < 0 || 经度分区数 > 5
	停止搜索这一条推特
else
	case 经度分区数 >= 0 && 经度分区数 <= 1
		1
	case 经度分区数 > 1 && 经度分区数 <= 2
		2
....

纬度分区数 = （纬度 - 38.1）/ 0.15
if 纬度分区数 < 0 || 纬度分区数 > 4
	停止搜索这一条推特
else
	case 纬度分区数 >= 0 && 纬度分区数 < 1
		A
	case 纬度分区数 >= 1 && 纬度分区数 < 2
		B
.....

具体分区的判断和分配函数
设置新的变量 推特分区
case 经度分区 = 1 && 纬度分区 = A
	推特分区 = A1
case 经度分区 = 2 && 纬度分区 = A
	推特分区 = A2
.....
Default:
	停止搜索这一条推特


2. 数据结构，一个object
推特ID，数字
推特定位名称，字符串，能够简化判断推特是否属于墨尔本地区？
推特的定位坐标，数组（含有两个浮点数，分别代表经纬度）
推特的内容，字符串

3. 搜索方式
这个问题我有两种思路
第一种方法是：先把推文分成单词后按字母排序，根据不同单词的首字母将他们放在不同的数组中，可以获得一个含有不同首字母单词组成的数组的数组，这样在搜索时就能够跳过情绪字典中一些首字母单词的搜索过程
第二种方法是：将情绪字典中的单词按首字母分类，组成一个以不同首字母单词组成的数组的数组，然后根据推文的不同单词搜索不同首字母的子字典，这样每次搜索时的范围比较小
字典中单词的具体搜索方法我倾向于使用Binary Search
